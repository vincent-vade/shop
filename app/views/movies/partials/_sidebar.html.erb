<div class="col-sm-3">
  <div class="list-group">
  	<h4>Catégories</h4>
  	<hr>
  	<%= link_to("Tout", movies_path, class: "list-group-item") %>
  	<% @categories.each do |category|%>
    <%= link_to(category.name.capitalize, category_path(category), class: "list-group-item") %> 
    <% end %>
  </div>
</div>